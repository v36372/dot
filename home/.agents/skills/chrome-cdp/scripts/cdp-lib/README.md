# Browser CDP internals

The canonical generic implementation lives in `packages/pi-skill-chrome-cdp/skills/chrome-cdp/scripts/cdp-lib`. Edit it there, then run `bun browser-cdp:sync` from the repository root to refresh the Pi Codex example copy. The package check rejects drift.
